<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="dbarackiralamaPU">

        <class>com.eazyrent.entity.Arac</class>
        <class>com.eazyrent.entity.Kisi</class>
        <class>com.eazyrent.entity.Kiralama</class>


        <properties>

            <!-- Postgresql database -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="org.postgresql.Driver"/>

            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:postgresql://localhost:5432/dbarackiralama"/>

            <!-- Credentials -->
            <property name="jakarta.persistence.jdbc.user"
                      value="postgres"/>
            <property name="jakarta.persistence.jdbc.password"
                      value="root"/>

            <!-- display SQL in console -->
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.hbm2ddl.auto" value="create"/>
            <!--    create: önce varsa tabloyu drop edip sonra create eder.
                    create-drop: önce varsa tabloyu drop edip sonra create eder, çıkarken de tekrar tabloyu drop edip çıkar.
                    update: ilk çalıştırmada tabloyu create eder. Daha sonrakilerde create etmez. Bu seçimde tablo asla drop edilmez.-->

        </properties>

    </persistence-unit>
</persistence>